# 🧠 SHawn Neural System

**D-CNS (Digital Central Nervous System)** - 신경계 기반 지능형 작업 관리 시스템

---

## 📂 폴더 구조

```
shawn_neural_system/
├── neural_model_registry.json    # 모델 점수 & 데이터 저장소
├── neural_router.py              # 신경계별 모델 자동 선택
├── work_tracker.py               # 작업 추적 & 리포트
├── reports/                      # 작업 리포트 저장
└── README.md                      # 이 파일
```

---

## 🚀 빠른 시작

### 1. 신경계별 모델 선택

```python
from neural_router import NeuralModelRouter

router = NeuralModelRouter()

# L1 뇌간 - 빠른 진단
result = router.select_model("L1")
# 출력: {"primary": "Groq", "score": 92.3, "neurotransmitter": "노르에피네프린"}

# L3 신피질 - 복잡한 분석
result = router.select_model("L3")
# 출력: {"primary": "Gemini", "score": 90.5, "neurotransmitter": "도파민"}
```

### 2. 작업 추적

```python
from work_tracker import WorkTracker

tracker = WorkTracker()

# 작업 시작
tracker.start_work(
    task_name="Workspace 정리",
    neural_levels=["L1", "L2", "L3", "L4"]
)

# 단계 실행
tracker.log_step(
    step_name="GitHub 진단",
    neural_level="L1",
    model="Groq",
    status="success",
    quality_score=98,
    response_time_ms=230,
    tokens_used=1200
)

# 작업 완료 & 리포트
report = tracker.end_work("completed")
```

---

## 🧬 신경계 구조

### Level 1: 뇌간 (Brainstem)
```
역할: 기본 진단 & 상태 확인
최적 모델: Groq (92.3%) → DeepSeek (85.7%) → Gemini (71.2%)
특징: 빠른 속도, 높은 안정성
```

### Level 2: 변연계 (Limbic System)
```
역할: 의사결정 & 우선순위 판단
최적 모델: Gemini (87.1%) → Claude (83.4%) → DeepSeek (78.2%)
특징: 정확한 판단, 감정 분석
```

### Level 3: 신피질 (Neocortex)
```
역할: 고급 사고 & 창의성
최적 모델: Gemini (90.5%) → Claude (91.2%) → DeepSeek (86.3%)
특징: 복잡 분석, 창의적 솔루션
```

### Level 4: 신경망 (NeuroNet)
```
역할: 라우팅 & 최적화
최적 모델: Gemini (89.2%) → DeepSeek (83.1%) → Claude (76.5%)
특징: 실시간 조정, 동적 최적화
```

---

## 📊 모델 신경전달물질 매핑

### Groq - 노르에피네프린 (Norepinephrine)
```
생물학적 역할: 감각 뉴런 + 소뇌
특징: 빠른 반응, 주의 집중, 스트레스 내성
성능: 속도 95%, 효율 85%, 가성비 95%
```

### Gemini - 도파민 (Dopamine)
```
생물학적 역할: 피라미드 세포 + 브로카 영역
특징: 보상 기대, 동기 부여, 창의성
성능: 속도 75%, 효율 94%, 가성비 70%
```

### Claude - 세로토닌 (Serotonin)
```
생물학적 역할: 성상세포 + 베르니케 영역
특징: 정서적 안정, 신중한 판단, 위험 회피
성능: 속도 65%, 효율 92%, 가성비 65%
```

### DeepSeek - 아세틸콜린 (Acetylcholine)
```
생물학적 역할: 운동 뉴런 + 해마
특징: 학습 속도, 기억 통합, 실용적 문제해결
성능: 속도 85%, 효율 88%, 가성비 85%
```

---

## 📈 평가 메트릭

### 3축 평가 시스템

```
속도 (Speed) - 35%
  ├─ 응답시간 (ms)
  ├─ 처리량 (tokens/sec)
  └─ 안정성 (uptime %)

효율 (Efficiency) - 45%
  ├─ 작업 완성도
  ├─ 요구사항 충족도
  ├─ 오류율
  └─ 인간 검증 점수

비용 (Cost) - 20%
  ├─ 토큰 당 비용
  ├─ API 성공률
  └─ 가성비
```

### 신경계별 가중치

```
L1 뇌간:     속도 50% + 효율 30% + 비용 20%
L2 변연계:   속도 30% + 효율 50% + 비용 20%
L3 신피질:   속도 20% + 효율 60% + 비용 20%
L4 신경망:   속도 40% + 효율 40% + 비용 20%
```

---

## 🔄 강화학습 알고리즘

### 점수 업데이트 공식

```
새 점수 = (기존 점수 × 학습률) + (성과 × (1-학습률)) + 보너스

학습률:
  ✅ 성공: 0.8 (기존 80% 반영)
  ⚠️  부분: 0.9 (기존 90% 반영)
  ❌ 실패: 0.5 (기존 50% 반영)
  🔴 API오류: 0.3 (기존 30% 반영)

보너스:
  ✅ 완전 성공: +10%
  ⚠️  부분 성공: ±0%
  ❌ 실패: -20%
  🔴 API한도초과: -30%
```

---

## 📋 데이터 구조

### neural_model_registry.json

```json
{
  "version": "2.0",
  "models": {
    "Groq": {
      "neurotransmitter": "노르에피네프린",
      "biological_role": "감각 뉴런",
      "levels": {
        "L1": {
          "score": 92.3,
          "trend": "+1.2",
          "success_rate": 0.98,
          "avg_response_ms": 230
        }
      },
      "daily_tokens": {
        "used": 8432,
        "limit": 10000
      }
    }
  },
  "neural_weights": {
    "L1": {
      "speed": 0.50,
      "efficiency": 0.30,
      "cost": 0.20
    }
  }
}
```

---

## 💡 고급 기능

### 작업 할당 리포트

```python
report = router.get_neural_allocation_report(
    task_name="복잡한 분석",
    neural_levels=["L1", "L2", "L3", "L4"]
)

# 출력:
# {
#   "task": "복잡한 분석",
#   "total_efficiency": 9.5,
#   "allocations": {
#     "L1": {"primary": "Groq", "score": 92.3},
#     "L2": {"primary": "Gemini", "score": 87.1},
#     "L3": {"primary": "Gemini", "score": 90.5},
#     "L4": {"primary": "Gemini", "score": 89.2}
#   }
# }
```

### 토큰 추적

```python
# 자동으로 추적됨
# - 일일 토큰 사용량
# - 토큰 한도 대비 사용률
# - 토큰 부족 시 자동 폴백
```

### 폴백 시스템

```python
# 주 모델 토큰 부족 시 자동으로 대체 모델 사용
# 우선순위: 1위 → 2위 → 3위 → 4위
# 모든 모델 토큰 부족 시: Groq 기본 폴백
```

---

## 🎯 사용 사례

### Case 1: 빠른 진단

```python
tracker.start_work("상태 확인", ["L1"])
tracker.log_step("진단", "L1", "Groq", "success", 98, 230, 1200)
report = tracker.end_work()
# 예상 효율: 9.2/10
```

### Case 2: 의사결정

```python
tracker.start_work("우선순위 결정", ["L2"])
tracker.log_step("분석", "L2", "Gemini", "success", 95, 1200, 3400)
report = tracker.end_work()
# 예상 효율: 9.5/10
```

### Case 3: 복잡한 분석

```python
tracker.start_work("프로젝트 계획", ["L3"])
tracker.log_step("계획", "L3", "Gemini", "success", 92, 1500, 4500)
report = tracker.end_work()
# 예상 효율: 9.2/10
```

### Case 4: 전체 통합

```python
tracker.start_work("전체 작업", ["L1", "L2", "L3", "L4"])
# 각 레벨별 단계 진행...
report = tracker.end_work()
# 예상 효율: 9.5/10
```

---

## 📊 성능 목표

```
평균 응답시간: 95ms
전체 효율: 9.43/10
처리량: 10K routes/sec
성공률: 99.2%+

월간 비용: ~$5
비용 절감: 99.98% ($25,000 → $5)
```

---

## 🔐 개발 vs 프로덕션

### 개발 중 (현재)
```
✅ 모든 신경계 활동 노출
✅ 모델 선택 이유 설명
✅ 상세 리포트 생성
✅ 학습 과정 표시
```

### 배포 후
```
✅ 최종 결과만 제공
❌ 신경계 활동 숨김
❌ 모델 선택 숨김
✅ 효율 점수만 표시
```

---

## 📝 라이선스

Private - SHawn-Brain 프로젝트

---

**만든이:** MoltBot  
**버전:** 2.0  
**마지막 업데이트:** 2026-02-01
